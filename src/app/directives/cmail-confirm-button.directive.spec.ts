import { CmailConfirmButtonDirective } from './cmail-confirm-button.directive';

describe('CmailConfirmButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CmailConfirmButtonDirective();
    expect(directive).toBeTruthy();
  });
});
