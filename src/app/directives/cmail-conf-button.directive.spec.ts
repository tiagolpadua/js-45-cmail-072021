import { CmailConfButtonDirective } from './cmail-conf-button.directive';

describe('CmailConfButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CmailConfButtonDirective();
    expect(directive).toBeTruthy();
  });
});
