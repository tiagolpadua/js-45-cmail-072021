import { FiltroPorAssuntoPipe } from './filtro-por-assunto.pipe';

describe('FiltroPorAssuntoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPorAssuntoPipe();
    expect(pipe).toBeTruthy();
  });
});
